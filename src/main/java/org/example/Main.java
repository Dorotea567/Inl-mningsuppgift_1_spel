package org.example;


import org.example.Race.TheRace;
import org.example.settings.Options;
import org.example.settings.Settings;

public class Main {
    public static void main(String[] args) {
        Options options = new Options();
        options.huvudmeny();
    }
}
